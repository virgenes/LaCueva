FROM node:20-alpine
# bot-worker: Discord bot only, no HTTP server

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

RUN npm prune --production

CMD ["node", "dist/bot-worker.js"]
